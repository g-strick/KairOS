---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - AGENTS.md
  - skills/help/SKILL.md
  - examples/daily/2025-01-01.md.example
  - examples/goals/weekly/example.md.example
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "A user reading AGENTS.md sees the project identity, the five-member crew roster, the active style pointer, and a skill quick-reference"
    - "AGENTS.md stays under 400 lines"
    - "A confused or new user can run /kairos-help and see the list of available commands and an explanation of the engine-vs-vault and crew-vs-council model"
    - "The engine ships instructive example files that demonstrate format without containing real personal data"
  artifacts:
    - path: "AGENTS.md"
      provides: "Canonical context root with the four D-09 sections"
      contains: "Crew"
      min_lines: 40
    - path: "skills/help/SKILL.md"
      provides: "/kairos-help skill — surfaces commands and explains the system model"
      contains: "kairos-help"
    - path: "examples/daily/2025-01-01.md.example"
      provides: "Example daily note showing frontmatter + format, clearly not real data"
    - path: "examples/goals/weekly/example.md.example"
      provides: "Example weekly goal file showing format"
  key_links:
    - from: "AGENTS.md"
      to: "skills/help/SKILL.md"
      via: "AGENTS.md skill quick-reference lists /kairos-help"
      pattern: "kairos-help"
    - from: "AGENTS.md"
      to: "styles/default.md"
      via: "AGENTS.md active-style pointer references styles/default.md"
      pattern: "styles/default"
---

<objective>
Populate the canonical AGENTS.md context root with its four required sections and ship the `/kairos-help` skill plus instructive example content. This makes the engine self-describing: a stranger who clones it can read AGENTS.md to understand the system and run `/kairos-help` to orient.

Purpose: AGENTS.md is the context root every agent and skill reads; its structure is permanent and must fit a 400-line budget (pitfall 3, context overload). `/kairos-help` is the welcome mat for confused or new users.
Output: A fully-populated AGENTS.md (replacing the plan-01 stub), the /kairos-help skill, two example files, and a .claude/CLAUDE.md that points Claude Code at AGENTS.md.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/KairOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/graysonstricker/Programs/KairOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@README.md
</context>

## Phase Goal

**As a** confused or brand-new KairOS user, **I want to** read one canonical context file and run one help command, **so that** I understand the engine-vs-vault and crew-vs-council model and know which commands exist — without reading the source.

<tasks>

<task type="auto">
  <name>Task 1: Populate AGENTS.md with the four canonical sections</name>
  <files>AGENTS.md, .claude/CLAUDE.md</files>
  <read_first>
    - AGENTS.md (the plan-01 stub — current state before you replace it)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 vault-root location + .claude/CLAUDE.md, D-09 the four required sections, D-10 under 400 lines)
    - README.md (The two layers of agents table — Crew roster; the Council distinction)
    - .planning/research/ARCHITECTURE.md (Crew agent table: Sorter/Steward/Scribe/Keeper/Concierge roles)
    - .planning/research/PITFALLS.md (pitfall 3 — AGENTS.md is a pointer file, not an encyclopedia; domain knowledge loads on demand)
  </read_first>
  <action>
    Replace the AGENTS.md stub with the four D-09 sections, in this order:
    1. Project identity block — vault name (KairOS), vault path (note it is injected at runtime by the session-start hook in Phase 4; for now state the default ~/kairos), current date placeholder note, and a backend-agnostic note (works under Claude Code or a local Ollama/Codex runner).
    2. Crew roster — one line per crew member: Sorter (triages inbox), Steward (checks days against aims, runs morning/evening), Scribe (weekly/monthly reflection), Keeper (hygiene/archive), Concierge (first contact/onboarding). Sourced from README and ARCHITECTURE.md.
    3. Active style pointer — point at styles/default.md, noting the style system is built in a later phase but the pointer lives here so skills know where to look (D-09 item 3).
    4. Skill quick-reference — list available /kairos-* commands with a one-line description each. In Phase 1 only /kairos-help exists, so list it; note that more commands arrive in later phases.
    Keep the whole file well under 400 lines (D-10). Do NOT inline domain knowledge (north-star explanation, full goal-hierarchy spec) — that loads on demand per pitfall 3.
    Create .claude/CLAUDE.md (D-08) as a thin file that points Claude Code at AGENTS.md as the canonical context root — a short note plus an @AGENTS.md style include/reference so Claude Code auto-detects it. Do not duplicate AGENTS.md content here.
  </action>
  <verify>
    <automated>[ "$(wc -l < AGENTS.md)" -lt 400 ] && grep -qi 'Crew' AGENTS.md && grep -q 'styles/default' AGENTS.md && grep -q 'kairos-help' AGENTS.md && test -f .claude/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains all five crew names: Sorter, Steward, Scribe, Keeper, Concierge
    - AGENTS.md contains the string styles/default (the active style pointer)
    - AGENTS.md lists /kairos-help in its skill quick-reference
    - AGENTS.md has fewer than 400 lines (wc -l < AGENTS.md < 400)
    - AGENTS.md mentions backend-agnostic operation (Claude Code or a local runner)
    - .claude/CLAUDE.md exists and references AGENTS.md (grep -q 'AGENTS.md' .claude/CLAUDE.md)
  </acceptance_criteria>
  <done>AGENTS.md contains the four canonical sections (identity, crew roster, style pointer, skill quick-reference) under 400 lines; .claude/CLAUDE.md points Claude Code at it; no domain knowledge is inlined.</done>
</task>

<task type="auto">
  <name>Task 2: /kairos-help skill and instructive example files</name>
  <files>skills/help/SKILL.md, examples/daily/2025-01-01.md.example, examples/goals/weekly/example.md.example</files>
  <read_first>
    - .planning/research/STACK.md (SKILL.md anatomy: purpose/trigger/process/output; skills/<name>/SKILL.md layout)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 /kairos-help ships in Phase 1; Claude's Discretion on examples/ content — minimal instructive examples that show format without being mistaken for real content)
    - README.md (system model: engine vs vault, crew vs council — the help skill explains this welcomingly)
    - AGENTS.md (so the help skill's command list matches the quick-reference)
  </read_first>
  <action>
    Create skills/help/SKILL.md following the STACK.md SKILL.md anatomy (purpose, trigger /kairos-help, process, output). The skill, when invoked, must: surface the list of available /kairos-* commands with one-line descriptions (currently just /kairos-help, noting more arrive in later phases), explain the engine-vs-vault model (the tool lives in the public engine; your life lives in the private vault), explain the crew-vs-council distinction (crew operate the system; council think with you on demand), and point to README for depth. Tone is welcoming and non-technical (D-11, the specifics note).
    Create examples/daily/2025-01-01.md.example — an example daily note using the frontmatter from STACK.md (date, week, focus, mood) with obviously-fake instructive content (e.g. a placeholder focus line), filename dated 2025-01-01 and suffixed .example so it can never be mistaken for a real daily note and is excluded from vault content. Keep it minimal.
    Create examples/goals/weekly/example.md.example — a minimal example weekly goal file showing the format a real weekly goal would take, clearly instructive, not real data.
  </action>
  <verify>
    <automated>test -f skills/help/SKILL.md && grep -q 'kairos-help' skills/help/SKILL.md && test -f examples/daily/2025-01-01.md.example && test -f examples/goals/weekly/example.md.example</automated>
  </verify>
  <acceptance_criteria>
    - skills/help/SKILL.md exists, names the trigger /kairos-help, and explains both the engine-vs-vault and crew-vs-council models (grep finds engine, vault, crew, council)
    - skills/help/SKILL.md's command list is consistent with AGENTS.md's skill quick-reference (both list /kairos-help)
    - examples/daily/2025-01-01.md.example exists and ends in .example (so .gitignore tracks it, not real content)
    - examples/goals/weekly/example.md.example exists and shows weekly goal format
    - Neither example file contains real personal data — content is placeholder/instructive
  </acceptance_criteria>
  <done>/kairos-help ships as a welcoming skill that lists commands and explains the system model; two .example files demonstrate daily-note and weekly-goal format without containing real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine repo → public remote | AGENTS.md, the help skill, and example files are committed to the public engine; any real personal data here would leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | examples/ content | mitigate | Example files use only placeholder/instructive content and the .example suffix; no real dates beyond the fixed 2025-01-01 demo, no real goals or names (Claude's-discretion guidance in CONTEXT.md) |
| T-02-02 | Information Disclosure | AGENTS.md content | mitigate | AGENTS.md holds only structural/system text (crew roles, pointers); domain knowledge and personal data load on demand from the vault and are never inlined (pitfall 3) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — markdown-only authoring |
</threat_model>

<verification>
- `wc -l < AGENTS.md` is under 400
- AGENTS.md contains all four D-09 sections (identity, crew roster, style pointer, skill quick-reference)
- `/kairos-help` skill file exists and explains the system model
- Example files exist with the .example suffix and contain no real data
</verification>

<success_criteria>
- AGENTS.md is the canonical, under-400-line context root with the four required sections
- A new user can run /kairos-help and understand the system without reading source
- The engine ships instructive examples that show format without risking a data leak
</success_criteria>

## Artifacts this phase produces

This plan produces:
- `AGENTS.md` — fully populated canonical context root (replaces plan-01 stub)
- `.claude/CLAUDE.md` — Claude Code pointer to AGENTS.md
- `skills/help/SKILL.md` — /kairos-help skill
- `examples/daily/2025-01-01.md.example` — example daily note
- `examples/goals/weekly/example.md.example` — example weekly goal file

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
