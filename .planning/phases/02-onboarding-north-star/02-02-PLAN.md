---
phase: 02-onboarding-north-star
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - skills/north-star/SKILL.md
  - skills/goals/SKILL.md
  - AGENTS.md
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05
must_haves:
  truths:
    - "Running /kairos-onboard, /kairos-north-star, and /kairos-goals is possible because all three skill files ship in the engine repo"
    - "skills/north-star/SKILL.md and skills/goals/SKILL.md are git-tracked (git ls-files returns both paths)"
    - "The Concierge agent and the three Council agents (Life Coach, Strategic Advisor, Accountability Partner) ship in the engine and are discoverable from AGENTS.md"
    - "AGENTS.md skill quick-reference lists /kairos-onboard, /kairos-north-star, and /kairos-goals"
    - "AGENTS.md has a Council section naming Life Coach, Strategic Advisor, and Accountability Partner"
    - "AGENTS.md stays under 400 lines"
    - "All 20 m002 checks pass (the full Phase 2 gate)"
  artifacts:
    - path: "skills/north-star/SKILL.md"
      provides: "/kairos-north-star skill (ONBD-04) — now committed to the engine repo"
      contains: "kairos-north-star"
    - path: "skills/goals/SKILL.md"
      provides: "/kairos-goals skill (ONBD-05) — now committed to the engine repo"
      contains: "kairos-goals"
    - path: "AGENTS.md"
      provides: "Canonical index updated with the three Phase 2 skills and a Council section"
      contains: "/kairos-onboard"
  key_links:
    - from: "AGENTS.md"
      to: "skills/north-star/SKILL.md"
      via: "skill quick-reference lists /kairos-north-star so agents know the skill exists"
      pattern: "/kairos-north-star"
    - from: "AGENTS.md"
      to: "agents/life-coach.md"
      via: "Council section names Life Coach so the council is discoverable from the canonical context root"
      pattern: "Life Coach"
---

<objective>
Ship the two previously-gitignored Phase 2 skills (`/kairos-north-star`, `/kairos-goals`) by committing them to the engine repo, and update the canonical `AGENTS.md` index so it lists all three Phase 2 skills and the Council. This turns all 20 M002 checks green — the complete Phase 2 success condition.

This plan depends on 02-01: the skill files cannot be committed until the `.gitignore` anchoring fix lands. The four Council/Concierge agent files and the onboarding skill are already committed (verified in research) — this plan adds the two remaining skills and reconciles the stale index.

Purpose: ONBD-04 and ONBD-05 require the north-star and goals skills to ship WITH the engine, not merely exist on one machine. ONBD-01/02/03 are already satisfied by committed artifacts but are only discoverable once AGENTS.md (the "map, not the territory" context root) names them. A stale index silently hides the entire Phase 2 capability from every agent and user that reads AGENTS.md first.
Output: Two committed skill files, an AGENTS.md with an updated skill quick-reference and a new Council section (under 400 lines), and a passing 20/20 M002 gate plus a clean full test suite.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/KairOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/graysonstricker/Programs/KairOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-north-star/02-RESEARCH.md
@.planning/phases/02-onboarding-north-star/02-01-PLAN.md
@AGENTS.md
@skills/north-star/SKILL.md
@skills/goals/SKILL.md
@test/m002.test.sh
</context>

## Phase Goal

**As a** new KairOS user who clones the engine, **I want to** receive the full onboarding + north-star + goals skill set and council that ship with the engine, **so that** I can run `/kairos-onboard`, `/kairos-north-star`, and `/kairos-goals` and have a council of agents available without authoring anything by hand.

This plan completes the slice: the enabling fix shipped in 02-01, and here the skills are actually committed and surfaced in the canonical index.

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit the two previously-gitignored skill files</name>
  <files>skills/north-star/SKILL.md, skills/goals/SKILL.md</files>
  <read_first>
    - skills/north-star/SKILL.md (verify content before committing — must contain ONLY template/default content, no real personal data accumulated while the file was untracked)
    - skills/goals/SKILL.md (same verification)
    - .gitignore (confirm 02-01 anchoring landed — git check-ignore must now exit non-zero for both skill paths)
    - .planning/phases/02-onboarding-north-star/02-RESEARCH.md (ONBD-04, ONBD-05 — these skills must ship with the engine)
  </read_first>
  <action>
    First confirm plan 02-01 landed: run `git check-ignore skills/north-star/SKILL.md` and `git check-ignore skills/goals/SKILL.md`; both must exit non-zero. If either still exits zero, STOP — 02-01 did not complete and this task cannot proceed.

    Information Disclosure check (T-02-04): before staging, read both files and confirm they contain ONLY template structure and placeholder defaults — generic example values like "Honesty, growth, connection" and "health.md / career.md", not any real name, timezone, or personal goal data. These files were authored as templates and sat untracked; verify nothing private accreted. If real personal data is present, do NOT commit — surface it for the user to scrub first.

    Stage both skill files: `git add skills/north-star/SKILL.md skills/goals/SKILL.md`. Confirm they are now in the index with `git ls-files skills/north-star/SKILL.md skills/goals/SKILL.md` (both paths must print). The executor's normal atomic-commit flow will produce the commit; the acceptance criterion is that both paths are tracked.

    Do NOT modify the content of either skill file — they are already well-designed and pass their behavior checks (verified in research). This task only brings them into version control.
  </action>
  <verify>
    <automated>git check-ignore skills/north-star/SKILL.md; test $? -ne 0 && git add skills/north-star/SKILL.md skills/goals/SKILL.md && git ls-files --error-unmatch skills/north-star/SKILL.md skills/goals/SKILL.md && echo TRACKED</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch skills/north-star/SKILL.md` exits zero (file is tracked)
    - `git ls-files --error-unmatch skills/goals/SKILL.md` exits zero (file is tracked)
    - Both skill files contain only template/default content (no real personal data) — confirmed by reading before staging
    - The content of both skill files is byte-identical to what was on disk before staging (no edits made)
  </acceptance_criteria>
  <done>Both Phase 2 skill files are staged and tracked by git, verified free of personal data, with their content unchanged — ONBD-04 and ONBD-05 now ship with the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md skill quick-reference and add a Council section</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (the file being changed — current skill quick-reference table lists only /kairos-help at lines 27-33; Crew table is at lines 7-15; no Council section exists)
    - skills/onboarding/SKILL.md (for the /kairos-onboard one-line description)
    - skills/north-star/SKILL.md (for the /kairos-north-star one-line description)
    - skills/goals/SKILL.md (for the /kairos-goals one-line description)
    - agents/life-coach.md, agents/strategic-advisor.md, agents/accountability-partner.md (for the Council one-line role descriptions)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 AGENTS.md sections, D-10 under 400 lines — the structural contract this file must keep)
    - .planning/phases/02-onboarding-north-star/02-RESEARCH.md (Pitfall 3 AGENTS.md staleness; D-P2-02 council accessibility — agents are accessible from the vault after BOTH setup.sh AND update.sh; no setup.sh change needed, document the two-step flow)
  </read_first>
  <action>
    Update the Skill Quick-Reference table (currently lists only `/kairos-help`) to add three rows, satisfying the m002 checks and Pitfall 3:
    - `/kairos-onboard` — Semi-structured onboarding interview; collects name, timezone, priorities, and habits, and confirms the vault is ready.
    - `/kairos-north-star` — Guided desired/dreaded future interview; writes `north-star/desired.md` and `north-star/dreaded.md`.
    - `/kairos-goals` — Guided setup of the goal hierarchy (5-year through weekly) with a linking index.
    Derive each one-line description from the skill's own Description section; keep each to a single table row. Remove or trim the "More /kairos-* commands arrive in later phases" note so it no longer claims onboarding is still pending.

    Add a new `## Council` section after the Active Style section. Introduce it with one sentence explaining the crew-vs-council distinction (crew = always-on cadence agents; council = on-demand advisors the user invokes). Then a table with three rows naming each council agent and its role, sourced from the agent files:
    - **Life Coach** — goal achievement and habit formation; invoked on demand.
    - **Strategic Advisor** — big-picture thinking and trade-off analysis; invoked on demand.
    - **Accountability Partner** — follow-through and commitment tracking; invoked on demand.
    The m002 gate greps for the literal string `Life Coach`, so that exact spelling must appear.

    Add one sentence (in the Council intro or a short note) documenting D-P2-02: the council agents ship in the engine `agents/` directory and become available in the vault after running `setup.sh` then `update.sh` (the two-step flow — `setup.sh` scaffolds the vault, `update.sh` copies the allowlisted `agents/` into it). This resolves the success-criterion-4 ambiguity by clarifying the mechanism rather than changing setup.sh.

    Keep AGENTS.md a concise index, not a manual — one line per skill, one line per council agent (D-10, "map not territory"). The file is currently 36 lines; after these additions it must stay well under 400.
  </action>
  <verify>
    <automated>grep -q '/kairos-onboard' AGENTS.md && grep -q '/kairos-north-star' AGENTS.md && grep -q '/kairos-goals' AGENTS.md && grep -q 'Life Coach' AGENTS.md && grep -q 'Strategic Advisor' AGENTS.md && grep -q 'Accountability Partner' AGENTS.md && [ "$(wc -l < AGENTS.md)" -lt 400 ]</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md skill quick-reference contains `/kairos-onboard`, `/kairos-north-star`, and `/kairos-goals`
    - AGENTS.md contains a Council section naming `Life Coach`, `Strategic Advisor`, and `Accountability Partner` (exact spellings)
    - AGENTS.md documents that council agents reach the vault via `setup.sh` then `update.sh` (D-P2-02 two-step flow)
    - `wc -l < AGENTS.md` is under 400
    - The Crew table and Active Style sections are preserved (not removed)
  </acceptance_criteria>
  <done>AGENTS.md lists all three Phase 2 skills, has a Council section naming the three council agents with the documented setup.sh+update.sh accessibility flow, preserves the existing Crew and Style sections, and remains well under 400 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the full Phase 2 gate and run the regression suite</name>
  <files>test/m002.test.sh</files>
  <read_first>
    - test/m002.test.sh (the gate — now 20 checks after 02-01 added two git-tracking assertions; this task runs it, does not edit it)
    - .planning/phases/02-onboarding-north-star/02-RESEARCH.md (Validation Approach — m002 is the phase gate; pre-push/setup/update must not regress)
  </read_first>
  <action>
    Run the M002 gate and confirm all 20 checks pass: `bash test/m002.test.sh` must exit zero and report 20 passed, 0 failed. The two git-tracking checks (added in 02-01) pass because Task 1 committed the skills; the two AGENTS.md checks pass because Task 2 updated the index.

    Run the full regression suite to confirm nothing else broke after the `.gitignore` and AGENTS.md changes: `bash test/setup.test.sh`, `bash test/pre-push.test.sh`, and `bash test/update.test.sh` must all exit zero. The setup and update tests exercise the vault scaffold and allowlist sync, which the `.gitignore` change could in principle affect; the pre-push test confirms the content-glob hook still behaves.

    If any test fails, do NOT mark the plan complete — capture the failure in the SUMMARY and stop. This task is the phase gate; passing it is the entire Phase 2 success condition.

    Do not edit test/m002.test.sh in this task — it was finalized in 02-01. This task only executes the suite.
  </action>
  <verify>
    <automated>bash test/m002.test.sh && bash test/setup.test.sh && bash test/pre-push.test.sh && bash test/update.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash test/m002.test.sh` exits zero and reports 20 passed, 0 failed
    - `bash test/setup.test.sh` exits zero (vault scaffold unaffected by the gitignore change)
    - `bash test/pre-push.test.sh` exits zero (content-glob hook still correct)
    - `bash test/update.test.sh` exits zero (allowlist sync unaffected)
  </acceptance_criteria>
  <done>The full 20-check M002 gate passes and the entire engine test suite (setup, pre-push, update) is green — Phase 2 success criteria are met with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file on disk → engine git history | Committing a file makes its content permanent and (eventually) public; anything personal in it would leak |
| AGENTS.md → every agent/user | AGENTS.md is read first by every agent and skill; a stale or wrong index misdirects the whole system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | committing skill files that sat untracked may contain accreted personal data | mitigate | Task 1 explicitly reads both files before `git add` and confirms they hold only template structure and generic placeholder defaults (no real name, timezone, or goal data). If real data is found, the task stops and surfaces it rather than committing. Research already verified the files are clean templates; this is the belt-and-suspenders check at commit time. |
| T-02-05 | Repudiation / Integrity | AGENTS.md drifts from the actual skill/agent set, silently hiding capabilities | mitigate | Task 2 reconciles the index against the on-disk skill and agent files; the m002 grep checks (and the under-400-line check) gate the change so the index cannot land stale. |
| T-02-06 | Tampering | a regression in setup/update/pre-push from the 02-01 gitignore edit slips through | mitigate | Task 3 runs the full suite (setup, pre-push, update, m002); the plan is not complete unless all four exit zero. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — pure git + Markdown edits only; nothing to verify. |
</threat_model>

<verification>
- `git ls-files skills/north-star/SKILL.md skills/goals/SKILL.md` returns both paths (skills committed)
- `bash test/m002.test.sh` reports 20 passed, 0 failed (full Phase 2 gate green)
- `grep -q 'Life Coach' AGENTS.md` and `grep -q '/kairos-onboard' AGENTS.md` succeed (index updated)
- `wc -l < AGENTS.md` is under 400 (D-10 budget preserved)
- `bash test/setup.test.sh && bash test/pre-push.test.sh && bash test/update.test.sh` all exit zero (no regressions)
</verification>

<success_criteria>
- All five ONBD requirements ship in the engine: Concierge (ONBD-01), `/kairos-onboard` (ONBD-02), Council agents (ONBD-03), `/kairos-north-star` (ONBD-04), `/kairos-goals` (ONBD-05)
- AGENTS.md is the accurate canonical index — lists all three Phase 2 skills and names the three council agents, under 400 lines
- The 20-check M002 gate passes completely
- The full engine test suite is green — no regression introduced by the Phase 2 changes
</success_criteria>

## Artifacts this phase produces

This plan produces (commits/modifies):
- `skills/north-star/SKILL.md` — `/kairos-north-star` skill, now committed to the engine (ONBD-04)
- `skills/goals/SKILL.md` — `/kairos-goals` skill, now committed to the engine (ONBD-05)
- `AGENTS.md` — skill quick-reference extended with the three Phase 2 commands; new Council section naming Life Coach, Strategic Advisor, Accountability Partner; documented setup.sh+update.sh accessibility flow

<output>
Create `.planning/phases/02-onboarding-north-star/02-02-SUMMARY.md` when done.
</output>
