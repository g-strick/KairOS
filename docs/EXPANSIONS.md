# Expansions

Ideas to add **after** `/capture` and `/daily` feel real in daily use. Pull from here when friction repeats — not before.

## Skills (candidates)

| Command | Purpose |
|---------|---------|
| `/sort` | Process `inbox/` — route items to `projects/`, `someday/`, `archive/`, or today's note |
| `/weekly` | Guided weekly review from past daily notes |
| `/evening` | Short end-of-day reflection |
| `/monthly` | Monthly reset and priority review |
| `/goals` | Goal hierarchy when projects aren't enough |
| `/status` | One-screen summary (avoid "dashboard" framing) |

## Vault paths (already scaffolded)

- `projects/` — active work
- `someday/` — inactive ideas
- `archive/` — retired items

## Still deferred

- `goals/` hierarchy — only if weekly review needs it
- `habits.md` — only if daily should track streaks
- macOS Calendar / Reminders integration
- Custom output styles
- Functional agent files if a skill benefits from a separate definition

---

*Promote one item at a time after the validation period.*
