---
name: kair-process-inbox
description: Deprecated — use /kair-triage. Redirects vault inbox processing to the unified triage skill.
disable-model-invocation: true
---

# /kair-process-inbox — Deprecated

**Use `/kair-triage` instead.** This command is kept so old habits and docs still resolve.

## Agent Instructions

Run the full `/kair-triage` workflow from `skills/kair-triage/SKILL.md`. The unified skill processes **vault `inbox/`** and **Reminders Inbox** (Reminders first). Do not use the legacy step-by-step Q&A flow.
