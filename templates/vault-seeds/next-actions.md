# Next actions

Actionable-now items. One unchecked task per line. Prefix an `@context` in the text (e.g. `@Computer`, `@Errands`).

```markdown
- [ ] @Computer Example action
```

Published to your execution bridge (Reminders Actions list) by `/kair-publish`.
