# Reference

Material worth keeping but not actionable. `/kair-triage` files reference captures here when the outcome is "keep for reference."

Use one note per topic (e.g. `reference/meeting-notes.md`). Do not create new GTD list files at the vault root — use `someday-maybe.md` for ideas to revisit later.
